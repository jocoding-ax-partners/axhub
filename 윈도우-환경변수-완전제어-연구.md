# Windows 환경변수 완전 제어 연구 (axhub CLI · plugin)

> 목적: `axhub-import-issues.md` 사건(2026-07-14, Windows)의 1번 문제 — "axhub 가 PATH 에 없음 / repair-path 는 이미 등록됐다는데 이 셸에선 안 됨" — 를 포함해, Windows 환경변수(PATH·HOME·설정 스위치)로 인한 실패군을 **구조적으로 소멸**시키는 방법을 연구해요.
> 기준 코드: ax-hub-cli v0.24.6 (main 569cb12), axhub plugin v1.10.28.

---

## 0. TL;DR

Windows 환경변수 문제는 버그 하나가 아니라 **4개 층위의 구조적 문제**예요:

| 층위 | 문제 | 현재 상태 | 결론 |
|---|---|---|---|
| L1. 설치 시 PATH 영속화 | install.ps1 이 PATH 를 등록하지 않음 — 게다가 출력하는 수동 안내 명령이 REG_EXPAND_SZ 파괴 패턴 | ❌ 갭의 생성자 | installer 가 repair-path 재사용으로 등록 + 현재 세션 반영, 파괴적 안내 삭제 (P0) |
| L2. 이미 열린 셸 반영 | 부모 셸 env 는 자식이 못 바꿈 (OS 제약) | △ 절대경로 우회 안내만 | "새 터미널" 요구를 없애는 절대경로 lane 을 기계 계약(JSON)으로 승격 (P0) |
| L3. HOME 부재 셸 (PowerShell/cmd) | HOME 단독 읽기 6곳 → cwd(".")·temp 로 새는 경로 | ❌ config/keychain 만 수리됨 | 공용 home resolver 로 전 크레이트 통일 (P1) |
| L4. env 스위치 전파 | `AXHUB_NO_*` 등 82개가 GUI 앱·훅에 전파 안 됨 | △ 2-계층 계약 문서화만 | config 파일 counterpart + doctor env 진단 (P1) |

핵심 통찰 2개:
1. **`irm https://cli.axhub.ai/install.ps1 | iex` 는 사용자의 현재 PowerShell 세션 안에서 실행**되므로, installer 가 `$env:Path` 를 직접 갱신하면 그 세션에서 즉시 axhub 가 실행돼요. 레지스트리 등록 + WM_SETTINGCHANGE 까지 함께 하면 "새 터미널" 조차 필요 없는 유일한 시점이 설치 순간이에요. 지금은 이 기회를 버리고 수동 안내만 출력해요.
2. 이미 열린 셸(특히 Claude Code 의 Git Bash 세션)은 어떤 수단으로도 밖에서 PATH 를 못 고쳐요. 그러므로 **에이전트 lane 은 PATH 를 아예 신뢰하지 않는 방향**(절대경로 계약)이 정답이고, 이미 `current_exe()`/`AXHUB_BIN` 으로 절반은 와 있어요 — 남은 절반은 repair-path/detect JSON 이 절대경로와 셸별 세션 스니펫을 돌려주는 것이에요.

---

## 1. 사건 복기 — 환경변수가 만든 실패 사슬

`axhub-import-issues.md` (D:\claude\20260714-first-claude-code, kanban.html 1개):

1. `axhub` → `command not found`. 실물은 `C:\Users\2103001\.axhub\bin\axhub.exe` 에 존재.
2. `axhub plugin-support repair-path` → "PATH는 이미 axhub 설치 경로를 포함해요" (`cli_state: on_disk_not_on_path`).
   - 해석: **레지스트리(HKCU\Environment) User Path 에는 등록되어 있으나**, 이미 떠 있는 Claude Code(Git Bash) 세션의 프로세스 PATH 는 세션 시작 시점 스냅샷이라 반영 불가.
3. 우회: 그 세션에서는 axhub.exe 절대경로 직접 호출로 진행 (사람이 즉석 판단).

이 사슬에서 환경변수가 만든 비용: 도구 실패 1회 + 오해 소지 메시지 1회 + 사람의 즉석 우회 1회. 같은 파일의 5번(GitHub device flow 3회 실패)도 "PowerShell 의 HOME 부재로 다른 곳의 토큰을 읽는" 류의 env 분기 오탐과 결합하면 진단이 더 어려워지는 구조였어요 (AP-13 이 "HOME 없는 PowerShell 의 미로그인은 실패가 아니에요" 라고 계약으로 못 박은 이유).

---

## 2. Windows 환경변수 동작 원리 (왜 이렇게 자주 깨지나)

Windows 환경변수는 3개의 서로 다른 저장소가 **한 방향으로만** 흐르는 구조예요:

```
[영속 저장소: 레지스트리]
  HKCU\Environment                (User 스코프)
  HKLM\...\Session Manager\Environment  (Machine 스코프)
        │  ① 로그인/프로세스 생성 시점에만 읽힘
        ▼
[프로세스 env 블록]  ← CreateProcess 가 부모의 블록을 복사 (스냅샷)
        │  ② 자식으로만 상속, 부모로 역류 불가
        ▼
[셸 세션 변수]  $env:Path / %PATH% / $PATH — 그 프로세스 안에서만 유효
```

이 구조에서 나오는 함정들:

**T1. 레지스트리 수정은 이미 떠 있는 프로세스에 절대 반영되지 않아요.**
프로세스 env 블록은 생성 시점 스냅샷이에요. `setx` 든 레지스트리 직접 수정이든, 이미 열린 cmd/PowerShell/Git Bash/Claude Code 는 그대로예요. 사건의 `on_disk_not_on_path` 가 정확히 이 상태예요. 자식 프로세스가 부모의 env 를 바꾸는 공식 수단은 존재하지 않아요 — 그래서 `repair-path` 가 프로세스 env 주입을 no-op 으로 둔 것은 올바른 설계예요.

**T2. "새 터미널" 의 효과는 부모가 누구냐에 달려 있어요.**
새 터미널은 기본적으로 **부모 프로세스의 env 를 상속**해요. 확정된 매트릭스 (§2.1-3):
- Explorer(작업표시줄·시작메뉴)에서 실행 → WM_SETTINGCHANGE 수신 후 재구성된 env 상속 ✅
- **Windows Terminal 1.18+ (2023-05)** → 새 탭/pane 생성 시 **레지스트리에서 env 재생성** (기본 on, `compatibility.reloadEnvironmentVariables`) ✅ — "새 탭이면 충분" 한 환경이 이미 다수
- **VS Code 통합터미널** → VS Code 메인 프로세스의 stale env 상속, **앱 재시작 전까지 새 터미널도 stale** ❌ (2018년부터 open 인 이슈)
- 이미 열린 콘솔(cmd/PowerShell/Git Bash) → 어떤 수단으로도 자동 갱신 없음 ❌

**T3. `setx` 는 3중으로 파괴적이에요.**
공식 문서 명시: (a) **1024자 절단** ("content is cropped… can result in loss of data"), (b) 기존 변수의 **`%VAR%` 참조를 제거하고 확장값으로 치환**, (c) `setx PATH "%PATH%;…"` 패턴은 machine+user 결합 PATH 를 user 값에 통째로 덮어씀. 그러고도 SUCCESS 를 출력해요. GitHub Desktop 인스톨러가 이걸로 사용자 PATH 를 잘라먹은 사고가 실재해요. 우리 repair-path 가 setx 대신 레지스트리 API + `GetValueKind` 보존 + raw 읽기(`DoNotExpandEnvironmentNames`)를 쓰는 이유예요.

**T4. HOME 은 Windows 의 표준 변수가 아니에요.**
PowerShell·cmd 에는 HOME 이 없어요(USERPROFILE 이 표준). Git Bash 는 nsswitch `db_home: env windows cygwin desc` 규칙으로 HOME(설정 시) → HOMEDRIVE+HOMEPATH → USERPROFILE 순으로 만들어줘요. 그래서 같은 명령이 Git Bash 에선 되고 PowerShell 에선 다른 경로를 보는 분열이 생겨요 — G3 의 근원. 참고로 Rust 생태계 표준(`home`/`dirs` 크레이트, std 1.85+)은 **Windows 에서 HOME 을 아예 읽지 않고** USERPROFILE→`SHGetKnownFolderPath` 를 써요.

**T5. GUI 앱은 shell profile 을 모르는 채로 떠요.**
`.bashrc`/`.zshrc` 의 `export` 는 그 셸 계열 자식에게만 흘러요. 시작메뉴에서 뜬 Claude Desktop·VS Code 는 로그인 세션 env(=레지스트리 유래)를 상속하므로, profile 에만 export 한 `AXHUB_NO_AUTO_UPDATE` 는 훅에 안 닿아요. Windows 에서 GUI 까지 닿는 사용자 스코프 설정은 레지스트리 User env 또는 **파일**뿐이에요 — G5 의 근원.

**T6. Git Bash(MSYS)는 인자·PATH 를 변환해요.**
Git Bash 는 시작 시 Windows PATH 를 POSIX 형식으로 변환해 자기 PATH 를 구성하고, native .exe 호출 시 `/foo` 형태 인자를 `C:\Program Files\Git\foo` 로 자동 변환할 수 있어요 (`MSYS_NO_PATHCONV` 로 억제). axhub 는 `--flag value` 스타일이라 대부분 안전하지만, 경로 인자를 다루는 스킬 계약에서 인지해야 할 배경이에요.

### 2.1 근거·세부 확정 (웹 리서치, 권위 소스 검증 완료)

**(1) 저장소·value kind**
- User env = `HKCU\Environment`, System env = `HKLM\System\CurrentControlSet\Control\Session Manager\Environment` — [.NET SetEnvironmentVariable 문서](https://learn.microsoft.com/en-us/dotnet/api/system.environment.setenvironmentvariable), [Win32 Environment Variables 문서](https://learn.microsoft.com/en-us/windows/win32/procthread/environment-variables)
- `%VAR%` 참조 포함 값은 REG_EXPAND_SZ 여야 확장돼요 — REG_SZ 는 리터럴로 읽혀요. rustup 초기에 winreg 가 REG_SZ 로 써서 PATH 를 오염시킨 실사고: [rustup#261](https://github.com/rust-lang/rustup/issues/261). Win10/11 기본 user Path 는 `%USERPROFILE%\...\WindowsApps` 를 포함해 REG_EXPAND_SZ 로 출고 → **kind 보존 필수** (repair-path 는 이미 준수).
- 유효 PATH = system Path + user Path append (system 우선) — [Chocolatey RefreshEnv.cmd](https://github.com/chocolatey/choco/blob/master/src/chocolatey.resources/redirects/RefreshEnv.cmd) 가 이 규칙을 미러링.

**(2) setx 3중 파괴 (공식 문서 명시)**
- "limit of **1024 characters** … content is **cropped** … can result in **loss of data**" + "removes any variable references and uses expanded values" — [setx 공식 문서](https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/setx)
- 실사고: [GitHub Desktop 이 setx 로 사용자 PATH 절단](https://github.com/desktop/desktop/issues/18176) (PR #18244 로 setx 제거)

**(3) WM_SETTINGCHANGE("Environment") 실제 수신 범위**
- 공식 계약: HWND_BROADCAST + `SendMessageTimeout`(hung 프로세스 블로킹 방지 — rustup 은 SMTO_ABORTIFHUNG 5000ms) — [WM_SETTINGCHANGE 문서](https://learn.microsoft.com/en-us/windows/win32/winmsg/wm-settingchange)
- **Explorer: 갱신함** → 이후 taskbar/시작메뉴에서 실행되는 앱은 새 env 상속 — [.NET 문서](https://learn.microsoft.com/en-us/dotnet/api/system.environment.setenvironmentvariable)
- **열린 콘솔(cmd/PowerShell): 갱신 안 함** — [Raymond Chen](https://devblogs.microsoft.com/oldnewthing/20150915-00/?p=91591)
- **Windows Terminal: 1.18(2023-05)부터 새 탭/pane 에서 레지스트리 기준 env 재생성 (기본 on)** — [WT 1.18 릴리즈 블로그](https://devblogs.microsoft.com/commandline/windows-terminal-preview-1-18-release/), [PR #14999](https://github.com/microsoft/terminal/pull/14999), 설정 `compatibility.reloadEnvironmentVariables`
- **VS Code 통합터미널: 앱 재시작 전 갱신 없음** — [vscode#47816](https://github.com/microsoft/vscode/issues/47816) (2018년부터 open/Backlog)

**(4) 프로세스 env block**
- "child process inherits the environment variables of its parent process" (CreateProcess 시 복사), 변수 하나 최대 32,767자, Vista+ 는 블록 전체 기술적 무제한 — [Win32 문서](https://learn.microsoft.com/en-us/windows/win32/procthread/environment-variables)

**(5) 이미 열린 셸의 인세션 refresh (Chocolatey refreshenv 원리)**
- 구현: Machine → User 순으로 레지스트리를 읽어 process env 재구성, PATH 는 `Machine;User` join, `USERNAME`/`PSModulePath` 등은 보존 — [EnvironmentHelper.cs](https://github.com/chocolatey/choco/blob/develop/src/Chocolatey.PowerShell/Helpers/EnvironmentHelper.cs)
- PowerShell 등가: `$env:Path = [Environment]::GetEnvironmentVariable('Path','Machine') + ';' + [Environment]::GetEnvironmentVariable('Path','User')`
- **caveat**: 세션 전용 PATH 추가분(venv activate, VS dev shell)을 잃어요 — [choco#1659](https://github.com/chocolatey/choco/issues/1659). → axhub `session_hint` 는 전체 재구성 대신 **install_dir append 한 줄**이 안전해요.
- cmd 는 dot-source 가 없어 "임시 배치 생성 → call" 이 유일한 일반해 — [RefreshEnv.cmd](https://github.com/chocolatey/choco/blob/master/src/chocolatey.resources/redirects/RefreshEnv.cmd)

**(6) Git Bash PATH 구성·재수입 불가**
- Git for Windows 의 `/etc/profile` 은 기본 `MSYS2_PATH_TYPE=inherit` → Windows PATH 를 POSIX 변환(`ORIGINAL_PATH`)해 상속 + MSYS 디렉토리 prepend — [git-sdk-64 /etc/profile](https://github.com/git-for-windows/git-sdk-64/blob/main/etc/profile)
- `exec bash -l` 도 레지스트리를 다시 읽지 않아요 — exec 은 environ 을 유지한 이미지 교체라 stale PATH 를 재료로 재조립할 뿐이에요. 인세션 해결은 `export PATH="$PATH:$(cygpath -u 'C:\...\bin')"` 수동 append 뿐.

**(7) HOME 해석 규칙 (셸·언어별)**
- Git Bash: nsswitch `db_home: env windows cygwin desc` — env schema 는 "HOME (falling back to HOMEDRIVE\HOMEPATH and USERPROFILE, in that order)" — [git-sdk-64 nsswitch.conf](https://github.com/git-for-windows/git-sdk-64/blob/main/etc/nsswitch.conf), [Cygwin ntsec 문서](https://cygwin.com/cygwin-ug-net/ntsec.html). git.exe 자체도 동일 계열 — [git commit e12a955](https://github.com/git/git/commit/e12a9556602f4d94254cc4eda25b5615ff07131a)
- Rust 생태계: `home::home_dir()` 는 Windows 에서 **USERPROFILE → SHGetKnownFolderPath(FOLDERID_Profile)**, HOME 을 절대 안 읽어요 — [docs.rs/home](https://docs.rs/home/latest/home/fn.home_dir.html). `std::env::home_dir()` 는 HOME-first 버그로 deprecated 였다가 **Rust 1.85 에서 USERPROFILE-first 로 수정, 1.87 un-deprecate** — [rust#132515](https://github.com/rust-lang/rust/pull/132515)
- 시사점: axhub 의 4단 fallback 은 HOME-first 라 Git Bash 관행과는 맞지만 Rust 생태계 표준과는 달라요. 사용자가 HOME 을 커스텀 설정한 Git Bash 에선 셸별 분열이 여전히 가능 → 사용자 노출 경로 표기는 `%USERPROFILE%\.axhub` 로 고정하는 게 안전해요.

**(8) MSYS 인자 자동 변환의 제어 수단**
- 공식 인정 한계: "not perfect and in corner cases converts arguments that look like Unix paths while they are not" — `/flag` 스위치·`key:/value`·콜론 리스트가 대표 오염 케이스 — [MSYS2 filesystem-paths 문서](https://www.msys2.org/docs/filesystem-paths/)
- 제어: `MSYS2_ARG_CONV_EXCL`(인자 제외), `MSYS2_ENV_CONV_EXCL`(env 제외), `MSYS_NO_PATHCONV=1`(Git for Windows 전용 전면 차단), `//flag` 이중 슬래시 회피.

---

## 3. axhub 표면 전수 감사 (v0.24.6 기준)

### 3.1 이미 견고한 것 ✅

| 표면 | 내용 | 근거 |
|---|---|---|
| home 4단 fallback | `HOME → USERPROFILE → HOMEDRIVE+HOMEPATH` | `crates/axhub-config/src/lib.rs:40-45`, `crates/axhub-keychain/src/lib.rs:376-381`, `plugin_support/support/runtime_paths.rs:74-92`, `plugin_support/preflight.rs:205-210` |
| repair-path (Windows) | setx 미사용, HKCU\Environment 직접 write, `DoNotExpandEnvironmentNames` raw 읽기, **value kind 보존**(REG_EXPAND_SZ 파괴 없음), 변경 전 백업 파일, WM_SETTINGCHANGE broadcast, powershell.exe 절대경로 해석(%SystemRoot%) | `plugin_support/repair_path.rs:229-352` |
| 자식 프로세스 바이너리 해석 | bare "axhub" 는 `current_exe()` 우선 — 자기참조 spawn 이 PATH 비의존. `AXHUB_BIN` override 지원 | `plugin_support/support/axhub_cli.rs:53,156-163` |
| update self-swap | `current_exe()` 기반 교체 + 프로세스 lock — PATH 비의존 | `crates/axhub-update/src/apply.rs:200-209` |
| 자식 env 통제 | `GIT_TERMINAL_PROMPT=0`, `GCM_INTERACTIVE=never` 주입으로 자식 git 의 인터랙티브 블로킹 차단 | `plugin_support/support/axhub_cli.rs` |
| AP-13 Windows 실행 계약 | Git Bash 전용, PowerShell 금지, 로그인 셸에서 auth 검증 — SessionStart 훅으로 always-on | `hooks/hooks.json` 3번째 entry, `docs/policy/agent-policy.md` |
| 플러그인 훅 | `$HOME`·`$OS` 만 사용 (Git Bash 가 HOME 보장), 네트워크·바이너리 무접촉 | `hooks/hooks.json` |

### 3.2 갭 (문제의 잔존 표면) ❌

**G1. install.ps1 / install.sh 가 PATH 를 등록하지 않아요** — 첫 갭의 생성자. **게다가 출력하는 수동 명령이 파괴적이에요.**
- `scripts/install.ps1:80-88`: PATH 에 없으면 `[Environment]::SetEnvironmentVariable('Path', [Environment]::GetEnvironmentVariable('Path','User') + ';…', 'User')` 명령을 **출력만** 하고 "then open a new terminal".
- **이 수동 명령 자체가 PATH 훼손 패턴이에요**: .NET `SetEnvironmentVariable` 은 값을 **REG_SZ 로 기록**해 기존 REG_EXPAND_SZ kind 를 파괴하고, `GetEnvironmentVariable` 읽기는 `%VAR%` 참조를 확장해 돌려주므로 참조가 리터럴 확장값으로 굳어요 — .NET 의 알려진 open bug ([dotnet/runtime#89695](https://github.com/dotnet/runtime/issues/89695), [#1442](https://github.com/dotnet/runtime/issues/1442)). Win10/11 기본 user Path 는 `%USERPROFILE%` 참조를 포함한 REG_EXPAND_SZ 라 실사용자에게 그대로 해당돼요. 즉 지금 안내를 충실히 따른 사용자일수록 PATH 의 다른 항목이 손상될 수 있어요.
- `scripts/install.sh:46-66`: 동일하게 copy-paste 힌트만 출력 (unix 쪽 힌트는 무해).
- 결과: 첫 설치 직후 100% 의 사용자가 `cli_path_missing` 상태로 시작 → repair-path → 새 터미널 안내 → 이탈 포인트.
- 아이러니: repair-path 에는 이미 kind 보존·백업까지 갖춘 안전한 레지스트리 등록 코드가 있는데, 설치 시점에는 안 쓰고 위험한 수동 명령을 안내해요.

**G2. repair-path 는 "이미 열린 셸" 을 못 고쳐요 (OS 제약)** — 그런데 절대경로 우회가 사람용 텍스트로만 존재해요.
- `repair_path.rs:92-96`: 현재 프로세스 PATH 주입은 의도적 no-op (Rust 2024 env mutation unsafe — 올바른 결정).
- JSON 계약 `{repaired, already_present, disabled, shell_rc, backup_path, install_dir, message, error}` 에 **실행 가능한 절대경로(`bin_path`)와 셸별 현재-세션 반영 스니펫이 없음** → 스킬/에이전트가 `install_dir + AXHUB_BIN_NAME` 을 재조립해야 하고, 온보딩 skill 은 "새 터미널을 열고 온보딩 계속" 으로 세션을 끊어요 (`skills/onboarding/references/install-channels-and-auth.md:40`).

**G3. HOME 단독 읽기 6곳 — PowerShell(HOME 없음)에서 cwd·temp 로 새요.**

| 위치 | 용도 | HOME 부재 시 동작 | 위험 |
|---|---|---|---|
| `axhub/src/commands/manifest.rs:345` | `--token-file` 경로가 cwd/home 안인지 검증 | home=None → cwd 밖 토큰파일 거부 | PowerShell 에서 정당한 `%USERPROFILE%` 하위 token-file 거부 |
| `axhub/src/commands/cache.rs:107-127` | strict cache 디렉토리 | `%TEMP%\axhub\cache\strict` 로 분기 | 셸에 따라 캐시 위치가 갈라짐 (Git Bash=`~/.axhub`, PowerShell=temp) |
| `plugin_support/deploy_prep.rs:171` | plugin 캐시 디렉토리 | `"."` → **프로젝트 폴더에 `.cache/axhub-plugin` 생성** | cwd 오염 |
| `plugin_support/config.rs:31` | preferences 경로 | `"."` → **프로젝트 폴더에 설정 파일 생성** | cwd 오염 + 설정 유실(폴더마다 다른 설정) |
| `plugin_support/preflight.rs:469` | last-deploy 캐시 fallback | `"."` | cwd 오염 |
| `axhub/src/commands/support.rs:224,452` | tracing.log 탐색·진단 번들 | temp_dir fallback | 진단 자료 못 찾음 |

- config/keychain(로그인 상태)만 수리된 상태라 "로그인은 되는데 캐시·설정·진단이 이상한" 2차 오탐군이 남아 있어요.

**G4. `axhub doctor` 에 env 진단이 없어요.**
- `doctor.rs` 는 네트워크·버전 중심. PATH(프로세스 vs 레지스트리 diff), HOME 해석 결과, config/tokens 실경로, 셸 종류를 보여주는 진단이 없어서 — AP-13 이 계약으로 막는 "PowerShell 미로그인 오탐" 같은 상황을 **사용자·에이전트가 검증할 도구가 없어요**.

**G5. env 스위치(82개)의 Windows 전파 문제.**
- CLI+plugin 의 동작 스위치가 전부 환경변수예요 (`AXHUB_NO_AUTO_UPDATE`, `AXHUB_DISABLE_PATH_REPAIR`, `AXHUB_NO_UPDATE_ROUTER`, `AXHUB_DEVICE_FLOW_AUTO_OPEN` …).
- Windows 에서 GUI 앱(Claude Desktop)이 띄우는 훅 bash 는 shell profile 을 소싱하지 않으므로 `.bashrc export` 가 안 닿아요 — CLAUDE.md 가 "2-계층 계약" 으로 문서화했지만, 이는 회피 설명이지 제어 수단이 아니에요.
- 영속·GUI-가시 설정은 레지스트리 User env 뿐인데 이를 안내하는 명령·문서가 없어요. env 와 동등한 **config 파일 counterpart** 도 없어요.

**G6. 문서-현실 불일치: npx setup lane.**
- AP-13·CLAUDE.md 는 `npx axhub@latest setup`(→ `axhub setup claude`)을 명문 예외로 규정하지만, npm 의 `axhub`/`axhub-cli` 는 **0.0.1 placeholder**(즉시 exit 1)이고 CLI 에 `setup` 서브커맨드도 없어요. 실재 설치 채널은 install.sh/install.ps1 뿐이에요. (Windows PATH 전략을 npx 가 해결해준다는 가정이 성립하지 않아요.)

**G7. "새 터미널" 안내가 통하지 않는 부모들 (확정 매트릭스는 §2 T2).**
- 통함: Explorer 발 터미널, Windows Terminal 1.18+ 새 탭 (레지스트리 재로딩 기본 on).
- 안 통함: **VS Code 통합터미널** (앱 재시작 필요), 이미 열린 콘솔, 그리고 **떠 있는 Claude Code 프로세스 자체** — Bash 도구 셸은 Claude Code 의 자식이라 Claude Code 를 재시작하기 전까지 stale 이에요. 사건의 상태가 정확히 이것이고, "새 터미널을 열어 주세요" 로는 **지금 세션의 온보딩을 이어갈 수 없는** 이유예요 → 계층 2(절대경로 lane)가 필수인 근거.

---

## 4. 타 도구 벤치마크

베스트-인-클래스 CLI 들의 공통점: **installer 가 PATH 를 직접 끝내고, "restart your shell" 안내에 현재 세션용 즉시 명령을 병기해요.** 사용자에게 레지스트리 등록을 시키는 도구는 없어요.

| 도구 | Windows PATH 전략 (검증된 구현) | 시사점 |
|---|---|---|
| rustup | `HKCU\Environment` 를 레지스트리 API 로 직접 수정, **REG_EXPAND_SZ 로 기록**(#261 사고의 교훈), `SendMessageTimeoutA(HWND_BROADCAST, WM_SETTINGCHANGE, "Environment", SMTO_ABORTIFHUNG, 5000)` — [windows.rs](https://github.com/rust-lang/rustup/blob/master/src/cli/self_update/windows.rs), [#261](https://github.com/rust-lang/rustup/issues/261) | 우리 repair-path 와 같은 접근 — 단 rustup 은 **설치 시점에** 해요 (P0-1 의 선례) |
| uv (cargo-dist) | `Set-ItemProperty -Type ExpandString` 으로 kind 보존 기록 + **더미 `[Environment]::SetEnvironmentVariable()` 호출로 브로드캐스트만 트리거**(.NET 의 REG_SZ 파괴 버그 회피) + "either restart your shell **or run:**" 현재 세션 명령 병기 — [installer.ps1.j2](https://github.com/axodotdev/cargo-dist/blob/main/cargo-dist/templates/installer/installer.ps1.j2), 옵트아웃 `UV_NO_MODIFY_PATH` — [uv 문서](https://docs.astral.sh/uv/reference/installer/) | 우리는 cargo-dist 산출물을 쓰면서 이 표준 등록 흐름만 뺀 상태. 옵트아웃 네이밍·현재-세션 병기 패턴 채택 |
| scoop | install.ps1 의 `Add-ShimsDirToPath` 가 `~\scoop\shims` 단일 디렉토리를 user PATH 에 1회 등록, 이후 앱마다 shim 생성 — [install.ps1](https://raw.githubusercontent.com/scoopinstaller/install/master/install.ps1) | "PATH 는 설치 1회만, 이후는 디렉토리 내용물" 패턴 — P2-2 근거 |
| volta / nvm-windows | 고정 shim(`%LOCALAPPDATA%\Volta\bin`)·symlink(NVM_SYMLINK) 디렉토리를 1회 등록, 전환은 내용물 교체 → **열린 콘솔에도 즉시 반영**(PATH 문자열 불변) — [volta 문서](https://docs.volta.sh/advanced/installers), [nvm-windows wiki](https://github.com/coreybutler/nvm-windows/wiki) | `~/.axhub/bin` 고정 디렉토리 + update self-swap 구조는 이미 이 패턴 — PATH 등록만 완성하면 동급 |
| winget | 같은 한계(호출 셸 미갱신)를 **메시징으로** 대응: "Path environment variable modified; restart your shell to use the new value." 자동 refresh 는 다년 미해결 요청 — [#531](https://github.com/microsoft/winget-cli/issues/531), [#1085](https://github.com/microsoft/winget-cli/issues/1085) | OS 벤더조차 열린 셸 문제는 못 풀어요 — 정직한 메시징 + 즉시 명령 병기가 상한선 |
| chocolatey | `refreshenv`/`Update-SessionEnvironment`: 레지스트리 Machine→User 재수입으로 현재 세션 재구성 (세션 전용 항목 소실 caveat) — [EnvironmentHelper.cs](https://github.com/chocolatey/choco/blob/develop/src/Chocolatey.PowerShell/Helpers/EnvironmentHelper.cs) | `session_hint`(P0-3)의 원형 — 단 axhub 는 안전한 **append-only 한 줄**로 충분 |

axhub 의 현재 위치: 저장 구조(고정 `~/.axhub/bin`)·수리 도구(repair-path)·자기참조 spawn(current_exe)은 이미 업계 표준 수준인데, **설치 시점 등록만 비어 있고 수동 안내는 파괴 패턴(G1)이에요**. P0-1/P0-2 는 새 발명이 아니라 rustup·uv 가 이미 검증한 표준으로의 수렴이에요.

---

## 5. 완전 제어 설계 — 5계층 방어

원칙: **환경변수를 "고치려" 하지 말고, 신뢰하지 않아도 되는 구조를 만들어요.** 고칠 수 있는 유일한 시점(설치 순간)에는 완결하고, 그 외 시점에는 절대경로·파일 기반으로 우회를 계약화해요.

### 계층 1 — 설치 시점 완결 (installer 가 PATH 를 끝내요)

지금의 설치 흐름은 "설치 → 수동 안내 → (무시) → cli_path_missing → repair-path → 새 터미널" 6단계예요. 설치 순간은 PATH 를 완결할 수 있는 **유일한** 시점이에요:

- **install.ps1**: `irm | iex` 는 사용자의 현재 PowerShell 세션에서 실행돼요. 따라서
  1. 방금 설치한 바이너리로 `& "$dest" plugin-support repair-path --json` 호출 → 레지스트리 등록·백업·**kind 보존**·WM_SETTINGCHANGE 가 전부 단일 소스(repair-path)로 재사용돼요. installer 에 레지스트리 로직을 중복 구현하지 않아요. (cargo-dist 도 동일 목표를 `Set-ItemProperty -Type ExpandString` + 더미 SetEnvironmentVariable 브로드캐스트로 풀어요 — 우리는 repair-path 재사용이 더 단순해요.)
  2. 추가로 `$env:Path += ";$InstallDir"` — **현재 셸 즉시 반영** (iex 는 현재 세션 스코프라 가능). 설치 직후 같은 창에서 `axhub --version` 이 바로 동작해요.
  3. **기존의 수동 안내 명령(.NET SetEnvironmentVariable 패턴)은 삭제**해요 — G1 에서 확인했듯 kind 파괴 + 참조 확장 패턴이라, 남겨두면 안내를 따르는 사용자가 PATH 를 훼손해요.
- **install.sh**: 동일하게 설치 직후 `"$dest" plugin-support repair-path --json` 호출 (unix lane 은 shell rc 등록 + 백업이 이미 구현돼 있어요). 현재 세션 반영은 `| sh` 파이프 특성상 불가능하므로, 마지막 줄에 `export PATH=...` 한 줄과 절대경로를 함께 출력해요 (uv 의 "either restart your shell or run:" 병기 패턴).
- 옵트아웃: `AXHUB_INSTALL_NO_PATH=1` (CI·패키징 환경). uv 의 `UV_NO_MODIFY_PATH`, 기존 `AXHUB_DISABLE_PATH_REPAIR` 와 일관된 네이밍으로.

효과: 첫 설치 사용자의 `cli_path_missing` 발생률이 구조적으로 0 에 수렴하고, PATH 훼손 안내가 사라져요. 사건의 1번 문제가 **발생 자체를 안 해요**.

### 계층 2 — 세션 내 즉시 사용 (PATH 불신 계약)

이미 열린 셸(특히 Claude Code Git Bash)은 밖에서 못 고쳐요. 그러므로 에이전트 lane 은 PATH 를 아예 안 써요:

- **repair-path JSON 확장**: `bin_path`(설치된 바이너리 절대경로), `current_session_stale: bool`(프로세스 PATH 에 없는데 레지스트리엔 있음), `session_hint: {powershell, cmd, bash}`(현재 세션 반영 스니펫) 필드 추가. 스니펫은 **append-only 한 줄**로 한정해요 — refreshenv 식 전체 재구성은 세션 전용 PATH(venv·vcvars)를 지우는 caveat 이 있어요(§2.1-5):
  - powershell: `$env:Path += ';C:\Users\X\.axhub\bin'`
  - cmd: `set "Path=%Path%;C:\Users\X\.axhub\bin"`
  - bash(Git Bash): `export PATH="$PATH:$(cygpath -u 'C:\Users\X\.axhub\bin')"`
- **onboarding detect 확장**: `cli_state: on_disk_not_on_path` 일 때 detect JSON 에 `bin_path` 포함 — 지금은 스킬이 canonical dir 을 하드코딩 휴리스틱으로 재추정해요 (`skills/onboarding/SKILL.md:90`).
- **스킬 계약 개정**: `cli_path_missing` 에서 "새 터미널을 열고 온보딩 계속" 으로 세션을 끊지 말고, **그 세션에서는 `bin_path` 절대경로로 후속 명령을 계속 실행**해요 — 사건에서 사람이 즉석으로 한 우회를 기계 계약으로 승격. 새 터미널 안내는 "다음 세션부터 짧은 명령으로 쓸 수 있어요" 보조 문구로 강등.
- **AP-13 개정**: "repair-path 후 새 터미널" → "같은 세션은 절대경로로 계속, 새 터미널은 영속 확인용". Windows 계약 훅 문구도 동기화.

근거가 되는 기존 자산: 자식 spawn 은 이미 `current_exe()` 우선(PATH 비의존)이고 `AXHUB_BIN` pin 이 있어요 — 이 설계는 그 원칙을 "에이전트 → 첫 진입 명령" 까지 확장하는 것뿐이에요.

### 계층 3 — HOME 통일 (단일 home resolver)

- `axhub-config` 의 4단 fallback(`HOME → USERPROFILE → HOMEDRIVE+HOMEPATH`)을 `pub fn resolve_home() -> Option<PathBuf>` 로 공개하고, §3.2 G3 의 6곳을 전부 이 함수로 교체해요.
- 정책: **`"."`(cwd) fallback 금지** — home 이 정말 없으면 (a) 읽기 경로는 None 처리, (b) 쓰기 경로(캐시)는 temp 로 통일하고 stderr 로 1줄 고지.
- **우선순위 주의 (마이그레이션 리스크)**: Rust 생태계 표준(`home` 크레이트, std 1.85+)은 Windows 에서 HOME 을 아예 안 읽어요(§2.1-7). 다만 우리는 이미 HOME-first 계약으로 출고했고(v0.24.x config/keychain), Git Bash 의 기본 HOME 은 결국 USERPROFILE 로 수렴하므로 대다수 사용자에겐 두 정책이 동일 경로예요. **HOME-first 유지가 안전**해요 — 지금 USERPROFILE-first 로 바꾸면 HOME 을 커스텀한 사용자의 기존 토큰·설정 위치가 이동해요. 대신 (a) 사용자 노출 경로 표기는 `%USERPROFILE%\.axhub` 로 고정하고, (b) `doctor env` 가 `home.source` 를 노출해 커스텀 HOME 분열을 진단 가능하게 해요.
- 효과: "로그인은 되는데 설정·캐시·진단이 이상함" 2차 오탐군 소멸. PowerShell 직접 실행(계약 위반이지만 현실적으로 발생)도 동작이 일관돼요.

### 계층 4 — 진단 가시화 (`axhub doctor env`)

env 문제의 최대 비용은 "무엇이 잘못됐는지 아무도 모른다" 예요. 진단 명령 하나로 가시화해요:

```
$ axhub doctor env --json
{
  "shell": "powershell" | "cmd" | "bash(git-bash)" | "bash" | "zsh" | ...,
  "home": {"resolved": "C:\\Users\\me", "source": "USERPROFILE", "HOME": null},
  "path": {
    "process_contains_install_dir": false,
    "registry_user_contains_install_dir": true,   // Windows 전용
    "registry_value_kind": "ExpandString",         // Windows 전용
    "verdict": "stale_session"                     // ok | stale_session | not_registered | not_installed
  },
  "dirs": {"config": "...", "tokens": "...", "cache": "...", "all_exist": true},
  "bin_path": "C:\\Users\\me\\.axhub\\bin\\axhub.exe",
  "advice": "이 셸만 오래됐어요. 지금은 bin_path 절대경로로 실행하고, 새 터미널부터는 axhub 로 쓸 수 있어요."
}
```

- `verdict: stale_session` 이 사건 1번의 상태를 **한 단어로** 판별해요 (지금은 repair-path 의 already_present 메시지를 사람이 해석해야 해요).
- 스킬·훅·서포트 문의가 전부 이 JSON 을 근거로 삼을 수 있어요. AP-13 의 "미로그인 오탐" 도 `home.source` 로 즉시 설명돼요.

### 계층 5 — 설정의 env 탈출 (config 파일 counterpart)

AXHUB_* 스위치는 코드 기준 82개예요. env 는 "프로세스 생성 시점 상속" 모델이라 Windows 에서 본질적으로 불안정해요 (GUI 앱·훅에 profile export 가 안 닿음 — CLAUDE.md 의 2-계층 계약이 그 증거).

- 주요 지속 스위치(auto_update, path_repair, update_router, onboarding_resume 등)에 `~/.config/axhub/settings.toml` counterpart 를 추가해요. 우선순위 `env > file > default` (env 는 일회성 override 로 유지).
- 훅(cheap bash, jq 없음)용으로는 TOML 파싱 대신 **marker 파일 관례**를 병행해요: 예) `~/.axhub/config/no-auto-update` 존재 = off. 훅 한 줄 (`[ -f ... ] && exit 0`) 로 검사 가능 — 기존 훅의 파일-존재 패턴과 동일한 비용.
- Windows 에서 env 로 꼭 써야 하는 경우를 위한 영속화 안내를 doctor env 와 문서에 포함: `[Environment]::SetEnvironmentVariable('AXHUB_NO_AUTO_UPDATE','1','User')` — **플래그성 단순 변수에는 이 패턴이 정석이에요** (HKCU\Environment 기록 + WM_SETTINGCHANGE 내부 브로드캐스트 확인됨, §2.1-3). PATH 처럼 `%VAR%` 참조가 있는 변수에만 금지(G1 의 REG_SZ 파괴 버그). GUI 앱은 재시작 후 반영.

### 계층 6 (선택) — 배포 채널 다변화

- **scoop/winget manifest**: scoop 은 shims 디렉토리가 이미 PATH 에 있어서 PATH 문제가 원천적으로 없어요. winget 은 Windows 사용자 관례와 정합. 둘 다 CDN zip 을 그대로 참조 가능해요.
- ~~npx lane 정합~~ → **npx lane 비지원 확정 (2026-07-14 결정)**: npx 셋업은 지원하지 않기로 했고, AP-13 의 npx 예외 조항을 정책·훅·CLAUDE.md 에서 전면 삭제했어요. 공식 설치 채널은 install.sh / install.ps1 뿐이에요.

---

## 6. 실행 로드맵

| 순위 | 항목 | 대상 | 변경 규모 | 갚는 갭 |
|---|---|---|---|---|
| **P0-1** | install.ps1: repair-path 호출 + `$env:Path` 현재 세션 반영 + **파괴적 수동 안내 삭제** (+`AXHUB_INSTALL_NO_PATH` 옵트아웃) | ax-hub-cli `scripts/install.ps1` | ~30줄 | G1 |
| **P0-2** | install.sh: repair-path 호출 + 마지막 줄 절대경로/export 안내 | ax-hub-cli `scripts/install.sh` | ~15줄 | G1 |
| **P0-3** | repair-path JSON 에 `bin_path`·`current_session_stale`·`session_hint` 추가 (하위호환: 필드 추가만) | ax-hub-cli `repair_path.rs` | ~60줄+테스트 | G2 |
| **P0-4** | onboarding detect JSON 에 `bin_path` 추가 + 스킬 계약 개정("새 터미널" → 절대경로 lane) + AP-13/훅 문구 동기화 | ax-hub-cli `preflight.rs` + plugin `skills/onboarding/*`, `hooks/hooks.json`, `docs/policy/agent-policy.md` | 중간 | G2, G7 |
| **P1-1** | `resolve_home()` 공개 + HOME 단독 6곳 교체 + env 매트릭스 테스트 | ax-hub-cli `axhub-config` + 6개 파일 | ~120줄+테스트 | G3 |
| **P1-2** | `axhub doctor env` (JSON/plain, Windows 레지스트리 비교 포함) | ax-hub-cli `doctor.rs` (신규 서브커맨드) | ~300줄+테스트 | G4 |
| **P1-3** | npx 문서 정합 (AP-13 미출시 명시) | plugin `docs/policy/agent-policy.md`, `CLAUDE.md` | 문서만 | G6 |
| **P2-1** | settings.toml counterpart + 훅 marker 파일 | ax-hub-cli + plugin hooks | 중간 | G5 |
| **P2-2** | scoop/winget manifest | 별도 repo/bucket | 소 | G1 보강 |

의존성: P0-3 → P0-4 (JSON 필드를 스킬이 소비). 나머지는 독립적이라 병렬 가능해요.

---

## 7. 검증 전략

1. **env 매트릭스 unit 테스트** (P1-1 과 함께): `HOME only / USERPROFILE only / HOMEDRIVE+HOMEPATH only / 전부 없음` 4조합 × 6개 교체 지점. 기존 config/keychain 테스트 패턴(`home_dir_supports_userprofile_when_home_missing`)을 재사용해요.
2. **Windows CI cross-check**: 기존 `x86_64-pc-windows-msvc` 테스트 job 이 매트릭스 테스트를 자연 커버. clippy cross-check 은 기존 절차 유지.
3. **installer runtime smoke** (P0-1/2 와 함께): CI 에서 install.ps1/install.sh 를 실제 실행 → **새 프로세스**에서 `axhub --version` 성공 + (Windows) 레지스트리 User Path 포함 + value kind 보존 assert. 참고: `review/ci-installer-runtime-smoke` 브랜치에 선행 작업이 있어요.
4. **doctor env 계약 테스트**: `stale_session / ok / not_registered / not_installed` 4 verdict 각각의 JSON snapshot.
5. **실기 QA**: Windows 실기(또는 VM)에서 사건 시나리오 재연 — ① 새 PowerShell 에서 `irm | iex` 직후 같은 창 `axhub --version` 성공(계층 1), ② Claude Code 세션(설치 전에 띄워둔 것)에서 onboarding → 절대경로 lane 으로 로그인까지 진행(계층 2), ③ PowerShell 직접 `axhub auth status` 가 Git Bash 와 동일 결과(계층 3).

---

## 8. 구현 현황 (2026-07-14, 같은 날 전체 구현)

로드맵 §6 의 전 항목을 구현해 PR 로 올렸어요.

| 항목 | 상태 | 근거 |
|---|---|---|
| P0-1/P0-2 installer PATH 완결 | ✅ [ax-hub-cli#494](https://github.com/jocoding-ax-partners/axhub-cli/pull/494) | install.ps1 이 repair-path 재사용 + `$env:Path` 현재 세션 반영 + 파괴적 수동 안내 삭제, install.sh 는 restart-or-run 병기. `AXHUB_INSTALL_NO_PATH=1` 옵트아웃. bats 8/8 |
| P0-3 repair-path 세션 계약 | ✅ #494 | JSON 에 `bin_path`·`current_session_stale`·`session_hint{powershell,cmd,bash}` (append-only). 단위 테스트 5종 |
| P0-4 detect·스킬 절대경로 lane | ✅ #494 + [axhub#386](https://github.com/jocoding-ax-partners/axhub/pull/386) | onboarding-detect `cli_resolved_path`, onboarding SKILL/reference/AP-13 훅/CLAUDE.md 개정 (구 CLI fallback 유지) |
| P1-1 resolve_home 통일 | ✅ #494 | `axhub_config::resolve_home_dir(_with_source)` 공개, HOME 단독 6곳 교체, cwd(".") fallback 소멸 |
| P1-2 doctor env | ✅ #494 | `verdict: ok\|stale_session\|not_registered\|not_installed`, home source, (Win) 레지스트리 포함·value kind. 실기 검증 완료 |
| P1-3 npx 문서 정합 | ✅ #386 | **npx lane 비지원 확정** — AP-13 npx 예외 조항을 정책·훅·CLAUDE.md 에서 전면 삭제 (G6 해소) |
| P2-1 스위치 파일 counterpart | ✅ #494 + #386 | `~/.axhub/config/<marker>` 관례 (env 에서 `AXHUB_` 뗀 소문자-하이픈, env 우선). CLI 2개 스위치 + 플러그인 훅/라우터 7개 전부 적용 |
| P2-2 scoop/winget | ✅ #494 | `packaging/` manifest 초안 + 배포 절차 README (v0.24.6 해시 고정, 릴리즈 자동 갱신은 후속) |

게이트: CLI `fmt`/`clippy -D warnings`/`nextest 3173`/`verify-cli-ux`/`shellcheck+bats`/`config Windows cross-clippy` 전부 통과, plugin `lint:tone 0 err`/`bun test 119` 통과. 설계 노트: P2-1 은 settings.toml 대신 **단일 marker 파일 관례**로 구현했어요 — 훅(cheap bash, jq 없음)과 CLI 가 같은 규칙을 읽고, TOML 파싱 의존이 생기지 않아요.

### 후속 2건 (같은 주간, 세션 재진입 + 실기 E2E)

- **세션 재진입 보완** ([ax-hub-cli#495](https://github.com/jocoding-ax-partners/axhub-cli/pull/495)→v0.24.8, [axhub#388](https://github.com/jocoding-ax-partners/axhub/pull/388)→v1.12.0): "세션 1 설치 성공 → 새 세션에서 재설치 권유" 오판을 소멸 — 세션 경계 표면이 자기 위치를 `~/.axhub/bin-path` 에 기록하고, DETECT 픽스처가 이를 1순위로 읽으며, Windows repair-path 가 레지스트리에 더해 `~/.bashrc` 도 병행 등록해요.
- **Windows 실기 E2E** (`.github/workflows/windows-e2e-live.yml`, `ci/windows-e2e-live` 브랜치, run 29407850617 **전 스텝 green**): windows-latest 에서 릴리즈된 CDN 자산으로 신규 설치→같은 창 즉시 사용(`axhub 0.24.9`)→레지스트리·bin-path·bashrc→새 세션 stale 재현→픽스처 lane(`already_present:true`·`verdict:"stale_session"`·절대경로 실행)→rc 이중화(**interactive=True login=True**)→옵트아웃까지 실측 검증했어요. 이 과정에서 **혼합 separator 로 인한 PATH 중복 append 실버그**를 발견·수정했어요 ([#496](https://github.com/jocoding-ax-partners/axhub-cli/pull/496)→**v0.24.9**: 후보 join 성분화 3곳 + PS 비교 정규화 8곳 + 회귀 가드 테스트).
