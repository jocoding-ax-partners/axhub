{
  "name": "@jocoding-ax-partners/axhub-plugin-cc",
  "version": "1.25.0",
  "description": "axhub plugin — Korean-first app operations and plugin marketplace. 11 skills (onboarding/bootstrap/scaffold/plugins/deploy/up/import/development/diagnosis/clarity/update) call ax-hub-cli directly with preview-confirm and terminal verification.",
  "type": "module",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint:tone": "bun scripts/check-toss-tone-conformance.ts --strict",
    "plugin:budget": "bun scripts/check-plugin-context-budget.ts",
    "plugin:bundle": "bun scripts/build-plugin-bundle.ts",
    "plugin:bundle:marketplace": "bun scripts/build-plugin-bundle.ts --out plugins/axhub",
    "plugin:bundle:codex": "bun scripts/build-plugin-bundle.ts --host codex",
    "plugin:bundle:codex:marketplace": "bun scripts/build-plugin-bundle.ts --host codex --out plugins/axhub-codex",
    "plugin:bundle:all": "bun run plugin:bundle:marketplace && bun run plugin:bundle:codex:marketplace",
    "plugin:budget:codex": "bun scripts/check-plugin-context-budget.ts --root plugins/axhub-codex",
    "plugin:validate": "bun run plugin:bundle && claude plugin validate dist/axhub-plugin",
    "release": "commit-and-tag-version",
    "release:tag": "bun scripts/release-tag.ts"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "commit-and-tag-version": "^12.7.1",
    "typescript": "^5.6.3"
  }
}
