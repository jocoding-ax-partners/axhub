# 셀트리온 테넌트 첫 배포 실패 조사 보고서

- **조사일:** 2026-07-07
- **대상:** `celltrion` 테넌트(id `cdaf483d-6f4d-4b53-8fe5-a7b01ffea2f5`) 첫 배포 실패
- **제보 내용:** "첫 배포가 실패한다. 파일 올라가는 시간보다 더 빠르게 배포 시도를 해서 실패하는 것 같다." / 추가: "그쪽 GitHub는 제한이 많다(웹 인증)." / "재배포하면 무조건 성공한다."
- **조사 범위:** 프로덕션 DB·백엔드 로그(1차) → 백엔드/CLI 소스 코드(2차 확정)

---

## 1. 결론 요약

1. **첫 배포 실패는 실재한다.** `celltrion`의 두 앱(`test`, `test2`) 모두 bootstrap(새 앱 만들기)의 첫 배포가 실패, 1~2분 뒤 재배포로 성공했다.
2. **원인은 백엔드 `bootstrap` saga의 결함이다. CLI가 아니다. (코드로 확정)** — 첫 배포가 **방금 push한 template 커밋의 SHA를 쓰지 않고, GitHub에 브랜치 HEAD를 독립적으로 다시 조회**한다. bootstrap repo는 항상 `auto_init` README 커밋과 template 커밋 두 개를 갖는 구조라, 이 재조회가 README 커밋을 집으면 배포가 "앱 코드 없는 커밋"을 빌드한다.
3. **사용자의 "파일 올라가는 시간보다 빠르게 배포한다"는 직관이 정확하다.** 파일 push(커밋 생성)와 배포 대상 결정(HEAD 재조회)이 별개의 GitHub API 호출이라 그 사이에 창(race window)이 있다.
4. **진짜 수정은 "빌드 파일 검증 게이트"(임시방편)가 아니라, HEAD 재조회를 없애고 push가 만든 정확한 SHA를 배포에 넘기는 것**이다.

> **확정 vs 추론 구분(중요):** "첫 배포가 push한 커밋 대신 HEAD를 재조회한다"는 **코드로 확정**. "그 재조회가 왜 셀트리온에서 README 커밋을 집었나(HEAD stale read / auto_init 전파 지연 / org repo 특성)"는 **강한 추론**이며 GitHub 내부 동작이라 코드만으로 단정하지 않는다.

---

## 2. 조사 방법

- MCP(axhub) 도구는 개발자 테넌트로만 스코프되어 셀트리온 직접 조회 불가 → **GCP 경로**로 조사(사용자 승인, 모두 읽기 전용).
- **DB** `axhub-prod-db`에 Cloud SQL Auth Proxy(IAM 터널)로 접근, **백엔드 로그**는 Cloud Logging(`worker`·`axhub-api-prod`)에서 조회.
- **정상 대조**는 `realitsyourman` 계정 repo를 gh CLI로 확인.
- **2차: 백엔드(Go)·CLI(Rust) 소스 코드**로 메커니즘 확정.

---

## 3. 확정된 사실 (DB·로그 증거)

### 3.1 배포 타임라인

| 앱 | 시각(UTC) | 결과 | trigger | commit | 실패 stage/code |
|---|---|---|---|---|---|
| test | 07-06 07:12:00 | **failed** | manual | `856a4196` | resolve / `resolve.spec_invalid` |
| test | 07-06 07:15:28 | succeeded | manual | `f3780b7b` | — |
| test2 | 07-07 02:26:33 | **failed** | manual | `4b7d2e00` | resolve / `resolve.spec_invalid` |
| test2 | 07-07 02:28:10 | succeeded | manual | `2009faa3` | — |

두 실패 모두 **bootstrap saga가 트리거한 첫 배포**다(`app_bootstraps.deployment_id` 일치).

### 3.2 실패 원인 (백엔드 `worker` 로그 원문)

```json
{
  "stage": "resolve",
  "code": "resolve.spec_invalid",
  "message": "빌드 설정을 해석하지 못했어요",
  "err": "dockerfile not found at repo root: railpack auto-detection failed: railpack did not detect a supported web app"
}
```

- **repo는 정상적으로 읽혔다**(root에 Dockerfile 없음을 *확인*) → 커밋을 못 찾은 게 아니다.
- 그 커밋에 **빌드할 앱 코드(Dockerfile·package.json)가 없었다.**

### 3.3 정상 테넌트 대조 (gh 직접 확인)

정상 앱 `realitsyourman/nextjs-starter` 첫 배포 커밋 `0b747a81`:
- 메시지 `"Initial commit from axhub template"`, `parents:0`, **파일 25개(Dockerfile 포함)** → 성공.

### 3.4 셀트리온 특이성

- bootstrap 첫 배포 **136건 중 130건 성공**, `resolve.spec_invalid` 첫 실패는 **셀트리온 2건이 전부**.
- 같은 saga·같은 `nextjs-axhub` 템플릿으로 다른 테넌트는 첫 배포가 정상 성공한다.

### 3.5 핵심 타이밍

`git_connection` 생성 → 첫 배포 시작 간격은 정상·셀트리온 모두 **~0.5초**(정상 0.50s / 셀트리온 0.46s). saga는 항상 repo 연결 직후 즉시 첫 배포를 트리거하며, 유일한 차이는 "그 시점에 main HEAD가 어떤 커밋이었는가"다.

---

## 4. 근본 원인 — 코드 레벨 인과 사슬 (백엔드)

파일: `internal/contexts/deploy/usecase/bootstrap/service.go`, `.../outbound/github/client_bootstrap.go`, `.../outbound/github/client_writes.go`, `.../usecase/deployment/trigger.go`, `.../outbound/github/client.go`

bootstrap saga는 순차 실행이다: `stageApp → stageRepo → stagePush → stageConnect → stageDeploy` (service.go:120-145).

1. **stageRepo — `CreateRepo(auto_init=true)`** (client_writes.go:87, bootstrap은 `Empty=false`)
   → GitHub이 repo 생성 시 **README만 있는 초기 커밋 `C0`**와 `main` 브랜치를 자동 생성.

2. **stagePush — `PushFiles`** (client_bootstrap.go:85-106)
   - template 파일들을 blob→tree→**부모 없는(orphan) 커밋 `C1`** 으로 생성.
   - `UpdateRef(main, C1, force=true)`로 main을 `C1`(파일 포함)으로 강제 이동.
   - **문제의 핵심 ①:** `PushFiles`는 `C1`의 SHA를 **계산해 놓고 반환하지 않는다** — `commitSha`는 로컬 변수이고 함수는 `error`만 돌려준다(client_bootstrap.go:98). 그래서 호출자(saga)는 방금 만든 커밋의 SHA를 모른다.

3. **stageDeploy — `Trigger(appID)`** (service.go:270 → deployment/trigger.go:32)
   - bootstrap은 commit을 넘기지 않는다(빈 값).
   - **문제의 핵심 ②:** commit이 비어 있으면 `resolveTriggerSha`가 **GitHub API로 main HEAD를 다시 조회**한다 — `ResolveCommitSha(repo, "main", "")` → `GET /repos/{repo}/commits/main` (trigger.go:106, client.go:153). 이 호출은 **그 시점의 브랜치 HEAD**를 실시간으로 가져온다(캐싱·다른 ref 아님, 확인함).

4. **결과:** 배포 대상 커밋 = "그 순간 GitHub이 main HEAD라고 응답한 커밋". 그게 `C0`(README)면 → resolve 단계에서 `railpack did not detect a supported web app` → **`resolve.spec_invalid`**.

### 확정된 결함 (GitHub이 무엇을 반환했든 성립)

> **첫 배포가 "방금 push해서 SHA까지 계산해 둔 정확한 커밋(`C1`)"을 재사용하지 않고, GitHub에 브랜치 HEAD를 독립적으로 다시 물어본다.** `PushFiles`가 SHA를 버리고(①) `Trigger`가 HEAD를 재조회하기(②) 때문이다. 두 GitHub 호출(ref 이동 → HEAD 조회) 사이에는 GitHub 쪽 상태가 아직 `C1`으로 보이지 않을 수 있는 창이 존재한다.

### 강한 추론 (특정 실패 모드 — 코드 밖, GitHub 내부)

`C0`(auto_init README)와 `C1`(template)은 부모가 없어 서로 무관한 커밋이고, main을 `C1`으로 옮기는 것은 non-fast-forward force 이동이다. 그 직후의 HEAD 재조회가 **아직 `C0`**를 받는 시나리오(HEAD stale read / auto_init 커밋 전파 지연 / org repo의 ref 전파 특성 중 무엇이든)이면 배포가 `C0`(README만)를 빌드한다 — 관측된 `resolve.spec_invalid`와 정확히 일치한다. **"왜 셀트리온(org repo)에서 이 창을 밟았고 개인 계정 테넌트는 안 밟았나"는 GitHub 내부 동작이라 단정하지 않는다.**

---

## 5. 사용자 가설 평가

| 제보 | 판정 | 근거 |
|---|---|---|
| 파일보다 빠르게 배포해서 실패 | **직관 정확** | 파일 push(커밋 생성)와 배포 대상 결정(HEAD 재조회)이 별개 API 호출이라 그 사이 창이 있음. 단 형태는 `commit-not-found`가 아니라 "직전 상태(README 커밋)로 배포" |
| 그쪽 GitHub는 제한이 많다 | **직접 원인 아님, 일부 열림** | `celltrion-it` org에 IP allow list 확인. 배포 *읽기*는 통과했으므로 읽기 차단은 아님. 다만 org repo라는 특성이 ref 전파 타이밍에 영향을 줬을 가능성은 추론으로 남음 |
| 재배포하면 무조건 성공 | **확인** | 재배포 시점엔 main HEAD가 `C1`으로 안정화 → 재조회가 올바른 커밋을 집음 |

---

## 6. 원인 귀속

- **CLI(Rust): 원인 아님.** `axhub apps bootstrap` 호출과 상태 폴링만 하며 commit 선택·배포 트리거에 관여하지 않는다(로그상 첫 배포는 saga가 서버 내부에서 트리거, CLI는 `GET app-bootstraps` 폴링).
- **플러그인 skill 층: 원인 아님.** saga 호출만.
- **백엔드 `bootstrap` saga: 원인 (확정).** push가 만든 정확한 커밋을 배포에 넘기지 않고 HEAD를 재조회한다.

---

## 7. 검증 안 된 두 지점 (정직한 기록)

1. **`4b7d2e00`(실패 커밋) = auto_init README `C0`인가** — 직접 확인은 셀트리온 repo 접근이 IP allow list로 막혀 불가. 단 **소거법으로 견고**: 첫 배포 시점에 존재한 커밋은 `C0`·`C1` 둘뿐이고, `C1`은 파일이 든 tree로 만들어졌으므로(비어 있으면 `FetchTemplateFolder`가 에러) fileless였다면 `C0`다. **판단을 막지는 않음.**
2. **`2009faa3`(성공 커밋) = template `C1`인가, 사용자가 새로 push한 커밋인가** — 이것이 남은 분기다.
   - `C1`이면 → 순수 타이밍 race이며 재조회가 안정화되어 self-resolving.
   - 사용자 push면 → `C1`이 애초에 main에 안 붙었다는 뜻(= `UpdateRef`가 org repo에서 무효였던 **별개 결함**).
   - 재배포가 `webhook`이 아니라 `manual`이었고 그 시점 webhook 배포가 없다는 점은 "새 push 없이 HEAD 재조회만 반복" = `C1`(self-resolving) 쪽에 약하게 기운다. **확정은 아니며 분기를 열어 둠.**

---

## 8. 권고

### 근본 수정 (권장)

- **`PushFiles`가 생성한 커밋 SHA(`C1`)를 반환하도록 바꾸고, `stageDeploy`가 그 SHA를 `Trigger`에 명시적으로 넘긴다.** `TriggerInput.CommitSha`는 이미 존재하므로(빈 값이면 HEAD 조회, 값이 있으면 그대로 사용 — trigger.go:99) **HEAD 재조회 경로를 타지 않게** 하면 된다. 이러면 위 §7의 근본 원인이 ①②③ 중 무엇이든 **창 자체가 사라진다.**
- 관련 지점: `client_bootstrap.go:98`(SHA 반환), `service.go:266-278`(SHA 전달), `trigger.go:98-111`(명시 commit 경로).

### 보완 (방어)

- **saga가 첫 배포 대상 커밋에 빌드 파일이 실제로 있는지 확인하는 게이트**(임시방편 — 위 근본 수정이 있으면 불필요하지만 방어선으로 유효).
- **bootstrap 성공 마킹 조건 강화:** 첫 배포가 fileless로 실패했는데 `app_bootstraps`가 `stage=done, error_code 없음`으로 남는 현재 동작 점검.

### 백엔드 팀 확인 요청

- `2009faa3`가 template `C1`인지 사용자 push인지(§7-2) — org repo에서 `UpdateRef` force 이동이 실제로 반영됐는지까지 함께 확인하면 근본 원인이 "순수 race"인지 "org write 경로 결함"인지 갈린다.

---

## 부록: 참조 식별자

- 테넌트 `celltrion` `cdaf483d-…` / 앱 `test` `803afff4-…`, `test2` `92a7f4c2-…`
- 실패 배포 `test` `9d944273-…`(`856a4196`) / `test2` `d15a9269-…`(`4b7d2e00`)
- 배포 GitHub App `ax-hub-deploy`(app_id 3745963), org `celltrion-it` installation `144696637`
- 코드 근거: `bootstrap/service.go:120-145,266-278` · `github/client_bootstrap.go:85-106` · `github/client_writes.go:87` · `deployment/trigger.go:98-111` · `github/client.go:153`
