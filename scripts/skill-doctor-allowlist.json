{
  "allows_dependency_execution": [
    {
      "skill": "setup",
      "rationale": "setup may run lockfile-based dependency install only after repo-on-disk detection, explicit consent, D1 guard, and mandatory --ignore-scripts to block postinstall execution."
    }
  ]
}
