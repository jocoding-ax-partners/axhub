{
  "input": {
    "command": "axhub auth login"
  },
  "expected": {
    "is_destructive": true,
    "action": "auth_login"
  },
  "description": "Auth login (mutates token storage at ~/.config/axhub/token)"
}
