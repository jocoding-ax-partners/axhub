{
  "input": {
    "command": "AXHUB_PROFILE=staging axhub deploy create --app paydrop --branch main --commit abc --json"
  },
  "expected": {
    "is_destructive": true,
    "action": "deploy_create"
  },
  "description": "AXHUB_PROFILE env override (staging profile)"
}
