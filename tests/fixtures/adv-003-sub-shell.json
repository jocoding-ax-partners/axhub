{
  "input": {
    "command": "$(axhub deploy create --app paydrop --branch main --commit abc --json)"
  },
  "expected": {
    "is_destructive": true,
    "action": "deploy_create"
  },
  "description": "Sub-shell wrapper $(...) bypass attempt"
}
