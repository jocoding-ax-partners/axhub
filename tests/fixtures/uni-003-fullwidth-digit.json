{
  "input": {
    "command": "axhub deploy create --app paydrop --branch main --commit abc１23 --json"
  },
  "expected": {
    "is_destructive": true,
    "action": "deploy_create"
  },
  "description": "Full-width digit '１' in commit sha — parser still flags destructive"
}
