{
  "input": {
    "command": "axhub deploy create --app pаydrop --branch main --commit abc --json"
  },
  "expected": {
    "is_destructive": true,
    "action": "deploy_create"
  },
  "description": "Cyrillic 'а' in app slug (homoglyph for ASCII 'a') — parser still flags destructive"
}
