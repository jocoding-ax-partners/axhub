# 수정 플랜 (합의 완료, 승인 대기) — Bootstrap 첫 배포 stale-HEAD race 제거

- **작성일:** 2026-07-07
- **대상 repo:** `/Users/wongil/Desktop/work/jocoding/ax-hub-backend` (Go 백엔드)
- **상태:** `pending approval` (ralplan 합의 완료, 실행 전 승인 필요)
- **합의:** Planner(Option A) → Architect(진행하되 2건 수정, 반영) → Critic(**APPROVE**, 1라운드 수렴)
- **연관 문서:** `셀트리온-첫배포-실패-조사.md` (근본 원인 조사)

---

## 결함 요약

bootstrap 첫 배포가 방금 push한 template 커밋(C1)을 재사용하지 않고 GitHub branch HEAD를 독립 재조회한다. `auto_init=true`로 만든 repo에는 README 커밋(C0)과 template 커밋(C1)이 공존하는데, race window에서 HEAD 조회가 C0(README만)를 잡으면 배포가 빈 앱을 빌드해 `resolve.spec_invalid`("railpack did not detect a supported web app")로 실패한다.

**핵심 통찰(Architect):** branch HEAD read(mutable pointer)는 wrong-but-valid(C0)를 침묵 반환해 결정론적 오배포를 만들지만, by-SHA read(immutable object)는 200 또는 404만 반환해 **틀린 커밋을 절대 주지 않는다**. 따라서 이 수정은 "빈번·결정론적 오배포"를 "희귀·자기소멸적 404"로 바꾸는 **strictly safer** 변경이다.

---

## 1. Principles

1. **Root-cause, not symptom** — HEAD 재조회 window 자체를 제거. 원인 불문 창이 없으면 실패 없음.
2. **이미 계산된 진실을 버리지 않는다** — `CreateCommit`이 반환한 C1(`client_bootstrap.go:98`)을 배포까지 전달만.
3. **Layer 규칙 불변** — usecase port는 primitive-typed(`commitSha string`), `TriggerInput` 매핑은 composition 담당.
4. **다른 트리거 경로 불변** — webhook·manual-HTTP는 별도 인터페이스로 이미 `CommitSha` 채움.
5. **Compile-time 강제** — 빈 문자열 default가 사고 원인. 파라미터로 강제해 "잊을 수 없게".

## 2. Decision Drivers

1. 프로덕션 첫-배포 실패 재발 없는 제거(최우선).
2. 최소 blast radius(prod 4파일, migration/domain/pg/routes 0).
3. 다운스트림 정합 — deployment saga `runResolve`의 모든 read(`stages.go:34,45,53`)가 `dep.CommitSha()` key → C1 주입 시 전 구간 by-SHA(결정적).

## 3. 선택안과 대안

### Option A (선택)
`PushFiles`가 C1 반환 → saga 지역변수 전달 → bootstrap `DeploymentTrigger.Trigger`에 `commitSha string` param → composition 어댑터가 `TriggerInput.CommitSha=C1`. deployment usecase는 non-empty CommitSha를 이미 그대로 씀(`trigger.go:99`) → 무변경.
- **Pros:** race 완전 소멸 · 다운스트림 결정적 · migration 0 · 영구 수정.
- **Cons:** port 시그니처 fan-out(어댑터 1 + fake 1).

### 탈락 대안
- **B — first-deploy build-file gate:** symptom-only(여전히 HEAD 배포, race 미해결) + railpack 판정 복제 brittle. A 위 방어층으로만 가능.
- **C — bootstrap도 empty-repo(auto_init=false):** `client_writes.go:29-32` — 빈 repo에 Git Data API는 `409 "Git Repository is empty"`. bootstrap이 auto_init=true 쓰는 바로 그 이유. push 메커니즘 재작성 필요, 위험 열위.
- **persist-to-aggregate:** bootstrap resume 경로 없음(`run()`은 in-memory `b`로 1회 실행, `Rehydrate`는 `GetByID` 폴링 전용). 소비자 없는 persist + "columns 1:1" invariant(`bootstrap.go:13`) 위반. → 지역변수 전달이 정답.

## 4. 구현 — 파일별 (prod 4 + test 3 = 7파일)

순서: 어댑터 반환 → port → usecase → composition → tests.

**① `outbound/github/client_bootstrap.go`** (`PushFiles` L85-106)
`...) error` → `...) (string, error)`. error 경로 `return "", ...`; 성공 `return commitSha, nil` (L98 기존 지역변수, 신규 계산 0).

**② `usecase/bootstrap/ports.go`**
- L62 `PushFiles(...) (commitSha string, err error)`
- L76 `Trigger(ctx, appID, startedBy, commitSha string) (id.DeploymentID, error)` (primitive 유지 → arch-lint OK)

**③ `usecase/bootstrap/service.go`**
- `stagePush`(L212-248) → `(string, error)`, `commitSha` 캡처·반환
- `run`(L130-134) `commitSha, err := s.stagePush(...)` 후 실패 시 compensate+markFailed
- `stageDeploy`(L266-278) param `commitSha` 추가 → `Deploy.Trigger(ctx, *b.AppID(), b.RequestedByUserID(), commitSha)`

**④ `composition/bootstrap_wire.go`**
- `bootstrapGitHub.PushFiles`(L101-103) 반환 passthrough
- `bootstrapDeployTrigger.Trigger`(L107-117) param `commitSha` + `TriggerInput{..., CommitSha: commitSha}`

**무변경:** webhook(`webhook/ports.go`+`module.go:943-952`), manual-HTTP(`deployment_handlers.go:50-52`), `deployment/trigger.go`·`stages.go`·`orchestrator.go`, domain aggregate, migrations, pg repos, routes.

## 5. 테스트 계획 (3 테스트)

**fix-1 — usecase thread-through:** `usecase/bootstrap/service_test.go`
`fakePorts`에 `pushedCommitSha`(sentinel)·`triggeredCommitSha` 추가. 신규 `TestRun_PassesPushCommitToDeploy`: happy path 후 `triggeredCommitSha == pushedCommitSha && != ""`. usecase seam 회귀 guard. 기존 5 fake-port 테스트는 시그니처만 맞춤.

**fix-2 — composition-adapter 테스트 (필수):** `composition/bootstrap_wire_test.go` (신규, 선례 `bootstrap_gitconn_test.go`)
- **왜 필수:** 실제 `bootstrapDeployTrigger` 어댑터를 커버하는 기존 테스트 0개. Go는 unused param 에러 없음 → `bootstrap_wire.go:108`에서 `CommitSha: commitSha` 누락해도 silent compile = race 재발. fix-1 fake는 실 어댑터를 우회하므로 이 gap을 못 잡음.
- **구성:** `bootstrapDeployTrigger{svc: deploymentusecase.NewService(Deps{...})}`, Trigger-path Deps만 채움(`Apps.Get`=subdomain 있고 ReviewApproved=false, `GitConnRepo.GetByApp`=connection, `Repo.Insert`=`*Deployment` 캡처, Notifier/Clock/Logger no-op, RunOnce 전용 port는 nil).
- **호출·assert:** `adapter.Trigger(ctx, appID, userID, "C1_SENTINEL")` → 캡처된 `dep.CommitSha() == "C1_SENTINEL"`.
- **결정적 assertion constraint (반드시 준수):** 올바른 어댑터는 `CommitSha="C1_SENTINEL"` → `resolveTriggerSha` 단락(`trigger.go:99`) → **Git 호출 0**. 잊은 어댑터는 `""` → `Git.GetInstallationToken`+`ResolveCommitSha(...,"")` 호출(`trigger.go:102-110`). 따라서 **로컬 Git fake는 어떤 호출에도 loud-fail(t.Fatalf/error 반환)** 하도록 wiring — 올바른 어댑터는 안 건드리니 통과, 잊은 어댑터는 즉시 실패. **`deployment/fakes_test.go:201`의 fakeGitClient(empty→"abc123def456") 재사용하거나 Git fake가 sentinel을 되돌려주면 forgot-field도 통과 = false pass** — 정확히 이게 막아야 할 test-theater.

**fix-3 — github 어댑터 반환:** `outbound/github/client_bootstrap_test.go` (신규, `client_writes_test.go` httptest 스타일)
`TestPushFiles_ReturnsCommitSha`: blobs→tree→commit→ref stub, `CreateCommit`→`{"sha":"commit-sha-1"}` → `PushFiles(...)` 반환 `"commit-sha-1"` assert.

**회귀 (webhook/manual):** 기존 `deployment/service_test.go` green 유지. 단 이 fix 커버리지로 세지 않음(별도 인터페이스, 경로 불변).

## 6. 검증 — 측정 가능한 gating criteria

PR 게이트에서 다음 4개 모두 green이면 수렴:
- **(a)** fix-2 composition 테스트가 `make test`(`-race -short`, PR 게이트) 통과
- **(b)** fix-1 bootstrap usecase 테스트 `triggeredCommitSha == pushedCommitSha` 통과
- **(c)** fix-3 github 어댑터 테스트 `PushFiles`가 `CreateCommit` SHA 반환 통과
- **(d)** `make verify`(`golangci-lint`+`go-arch-lint`) green — primitive-string-port 규칙 유지 증명

**staging-e2e:** labeled **non-blocking smoke backstop**으로만(credential 없으면 silent skip + race 결정론 재현 불가라 회귀 guard 아님). (a)-(d)가 회귀 guard.

## 7. 롤아웃 / 리스크 (pre-mortem)

1. **by-SHA eventual consistency — LOW-prob × TERMINAL-impact.** `CreateCommit(C1)` 직후 resolve 단계 `GET /commits/C1`(`stages.go:34`)이 GitHub 복제를 앞질러 404. 가능성 LOW(commit object 동기 생성, by-SHA는 강정합). BUT impact terminal: resolve 실패는 재시도 없이 `failStage` 종결(`orchestrator.go:161-185`). **잔여를 문서화된 ceiling으로 수용** — ceiling 주석만(`// ponytail: by-SHA 404 ceiling; retry는 deployment saga 전체로 blast radius 확대 + 진짜 commit_not_found masking이라 넣지 않음`). **resolve retry는 넣지 말 것(YAGNI).** fix 후 trigger 시점 read는 zero라, 잔여 window는 오늘의 trigger-time race보다 strictly 늦고(≈Insert 후 worker poll tick) 작음.
2. **미열거 caller 컴파일 깨짐 — CLOSED.** grep으로 `PushFiles` 5 ref · bootstrap `Trigger` 2 구현 전부 확인. param 추가는 compile-time 강제.
3. **어댑터 CommitSha 세팅 누락 no-op — CLOSED by fix-2.** 필수 composition 테스트(Git fake loud-fail)가 forgot-field를 결정적으로 fail시킴.

## Rationale (load-bearing)

- **Fix 후 trigger 시점 GitHub commit read = zero.** `resolveTriggerSha`가 non-empty CommitSha에서 단락(`trigger.go:99`) → trigger-time HEAD GET 사라짐. 유일 잔여 read는 resolve 단계 by-SHA(Insert ≈5 DB왕복 + worker poll tick 뒤).
- **C1은 Trigger의 deployment row insert 순간 durable.** `deploymentdomain.New(...,CommitSha:sha)` → `Repo.Insert`(`trigger.go:75,84`); CommitSha 컬럼 이미 존재 = **no migration**. resumable deployment saga가 `dep.CommitSha()`로 C1 읽음. **local-var hop은 stagePush→stageDeploy→Insert 사이 in-process-sequential 구간뿐** — Insert 후 durable. 이게 local-var 접근이 안전한 이유(persist 불필요).

## ADR

- **Decision:** `PushFiles`가 생성 커밋 SHA(C1)를 반환, bootstrap saga가 지역변수로 `stageDeploy`까지 전달해 `DeploymentTrigger.Trigger`에 명시 commit으로 넘김(Option A). deployment usecase·domain·migration·webhook 무변경.
- **Drivers:** 첫-배포 실패 영구 제거 · 최소 blast radius(prod 4파일) · 다운스트림 by-SHA 정합.
- **Alternatives considered:** B(symptom-only) · C(`409 empty` 제약) · persist-to-aggregate(resume 경로 부재 + 1:1-columns invariant).
- **Why chosen:** HEAD 재조회 window를 코드 경로에서 제거 → 원인 불문 재발 불가. port 파라미터화로 미래 경로가 SHA를 못 잊음.
- **Consequences:** bootstrap `DeploymentTrigger`/`GitHubClient.PushFiles` port 시그니처 변경(어댑터·fake fan-out); 신규 composition-adapter 테스트가 wiring 회귀를 PR 게이트에서 고정.
- **Follow-ups:** by-SHA 404는 ceiling 주석만(retry 금지) · B의 build-file gate는 원하면 A 위 방어층으로만.

### Open Questions (non-blocking)
- pre-existing: 프로세스가 `stagePush`와 `stageDeploy` 사이 크래시하면 bootstrap row가 `Pushed`/`Connected`로 고아화(resume worker 없음). 이 fix가 유발·악화하지 않음. 향후 reaper 검토 가능.
