#!/usr/bin/env bash
# SessionStart entry 3: AP-14 update-first Code-mode 라우팅 폴백 훅이에요.
# hooks.json 인라인 command 를 동작 불변으로 옮긴 wrapper 예요 — kill switch
# (env·marker)만 보고 항상 suppressed JSON 가드를 발행해요. 명령 실행·네트워크
# 접근은 없어요.

[ -n "$AXHUB_NO_UPDATE_ROUTER" ] && exit 0; [ -f "$HOME/.axhub/config/no-update-router" ] && exit 0; printf '%s\n' '{"continue":true,"suppressOutput":true,"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"[axhub] update-first routing guard is active for Code mode. If the current user prompt mentions axhub with 최신/버전/업데이트/latest/up to date/version check/update/upgrade, the first visible assistant text must be exactly: 현재 버전을 확인할게요. Invoke the axhub update skill before Finding tools, App/MCP tools, project probes, shell version probes, clarity, bootstrap, deploy, /oh-my-claudecode:autopilot, unrelated plugin modes, or generic Code-mode handling. Plugin version lookup command must be exactly claude plugin list; never claude plugin list 2>&1 or a redirected variant. After update, continue remaining app status/create/deploy/GitHub reconnect-device-code requests in the same conversation. Once the assistant says 업데이트 확인은 끝났어요, the remaining app-status flow must not run command-v, axhub --version, claude plugin list, plugin-location checks, or version probes again. For GitHub reconnect/device-code after update, apply the axhub clarity device-flow contract inline; do not invoke /axhub:clarity or show a failing skill badge; never run axhub git_connection_status, axhub github status, axhub --help | grep, shell pipes, or generic status probes; use exactly AXHUB_DEVICE_FLOW_AUTO_OPEN=1 axhub --no-input github link then axhub github accounts list --json; when the earlier device code was lost or expired, use AXHUB_DEVICE_FLOW_AUTO_OPEN=1 axhub --no-input github link --fresh instead, never while a valid code is still on screen, with tenant option only when already known, shell tool titles and descriptions exactly 계정 인증 시작 and 인증 확인. The assistant body must print exactly two normal chat lines with the URL in inline code: 인증 URL: `https://github.com/login/device` and 입력 코드: <USER_CODE>; never print the URL as a bare auto-link or Markdown link such as [https://github.com/login/device](github.com/login/device). Do not finish after showing the code, after pending text, or after telling the user to approve; continue to 인증 확인 in the same assistant turn, and never wait for the user to say 승인했어 or 배포 상태 확인해줘. For app status overview, allowed visible commands are exactly axhub apps --help then axhub apps list --json; if folder/current conversation/latest list clearly identifies a related app, do not ask which app and continue with CLI-only axhub apps get <app> --json and axhub deploy list --app <app> --json before any summary; never call Claude Desktop axhub App/MCP tools such as Deployment list (axhub), App get (axhub), or Tenant recent deployments (axhub); never run nonexistent singular axhub app list or axhub deployment list; never run command -v axhub && axhub --version, command -v claude && claude plugin list, or claude plugin list 2>&1 | grep; never add sleep, &&, pipes, redirects, head, tail, grep, sed, awk, bash -lc, sh -c, or 2>/dev/null."}}'
